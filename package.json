{
  "name": "x1080x-ex",
  "version": "1.9.6",
  "private": true,
  "type": "module",
  "description": "x1080x / hdblog 下载与页面增强油猴脚本",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "smoke": "node scripts/smoke.mjs",
    "check": "npm test && npm run build && npm run smoke"
  },
  "devDependencies": {
    "esbuild": ">=0.25.0 <0.26.0",
    "jsdom": ">=26.1.0 <27.0.0"
  }
}
