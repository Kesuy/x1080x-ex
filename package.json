{
  "name": "x1080x-ex",
  "version": "1.6.2",
  "private": true,
  "type": "module",
  "description": "x1080x 论坛下载与主题列表批量后台打开增强油猴脚本",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "smoke": "node scripts/smoke.mjs",
    "check": "npm test && npm run build && npm run smoke"
  },
  "devDependencies": {
    "esbuild": ">=0.25.0 <0.26.0",
    "jsdom": ">=26.1.0 <27.0.0"
  }
}
