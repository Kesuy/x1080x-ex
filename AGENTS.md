# x1080x-ex 开发约定

本文件是本仓库的长期开发规范。后续无论由人工、Codex、ChatGPT 或其他自动化工具修改项目，都应遵守以下约定。

## 新功能必须提供开关

凡是**用户可感知、可独立启用或关闭的新功能**，默认都必须提供独立设置开关，不能直接写死为永久启用。

例如：新增按钮、页面增强、自动跳转、自动查询、外部请求、批量操作、下载行为、内容过滤、样式增强、第三方服务集成等，都应有对应开关。

### 开关放置位置

- `agaghhh.cc` / x1080x 相关功能：放到 **⚙️ x1080x 设置**。
- `hdblog.me` 相关功能：放到 **⚙️ hdblog 设置**。
- qBittorrent 或其他独立服务相关功能：放到所属功能的设置区域中。
- 不同网站的设置不得混在同一个站点设置面板中。

### 默认行为

- 新增功能如果原本就是默认启用行为，新增开关后应默认 **开启**，避免升级后现有用户行为突然改变。
- 如果新增功能本身设计为可选、实验性或存在明显副作用，可以默认关闭，但需要在说明或 Release 中明确标注。
- 读取旧版本设置时必须提供合理默认值，保证升级兼容。

### 功能依赖

如果功能 B 依赖功能 A：

- B 仍应保留自己的独立开关；
- 可以在 A 关闭时将 B 的控件禁用，并在界面中说明依赖关系；
- 不要因为存在依赖，就把多个用户可感知功能合并成一个无法单独控制的总开关。

### 不需要单独开关的情况

以下修改通常不需要额外增加开关：

- Bug 修复；
- 网站兼容性修复；
- 安全修复；
- 性能优化；
- 纯代码重构；
- 文件名解析、请求封装、缓存等没有独立用户行为的底层实现；
- 已经明确属于某个现有功能内部实现、关闭该上层功能后不会单独运行的辅助逻辑。

如果不确定一个新能力是否应该单独做开关，默认按“**需要开关**”处理。

## 实现要求

新增功能开关时应同时做到：

1. 使用独立、稳定的 `GM_getValue` / `GM_setValue` 存储键；
2. 设置名称能够直接说明控制的功能，不使用含糊的“高级功能”“其他增强”等名称；
3. 关闭开关后，该功能不应继续执行 DOM 修改、网络请求、自动跳转或后台任务；
4. 不应因为关闭某个开关而误伤其他互相独立的功能；
5. 保存设置后，如功能无法即时安全切换，可以刷新页面后生效；
6. 新增或修改开关时同步更新 README 的设置说明。

## 测试要求

新增独立功能时，至少应覆盖以下行为中的适用项：

- 开关默认值符合设计；
- 开启时功能生效；
- 关闭时功能不运行；
- 与相邻独立功能互不干扰；
- 旧版本没有该设置键时仍保持兼容。

提交前运行：

```bash
npm run check
```

也就是依次通过测试、构建和 smoke 检查后再发布。

## 发布前检查

每次新增用户可感知功能，在发布前确认：

- [ ] 已提供对应功能开关；
- [ ] 开关位于正确的设置面板；
- [ ] 默认值不会破坏旧用户行为；
- [ ] 关闭后功能真正停止执行；
- [ ] 已补充必要测试；
- [ ] README / Release Notes 已同步说明。

**核心原则：用户可感知的新功能，除非属于现有功能不可分割的内部实现，否则都应该让用户能够单独关闭。**
